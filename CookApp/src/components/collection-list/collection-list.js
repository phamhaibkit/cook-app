import React, { Component } from 'react';
import { Text, View } from 'react-native';
import { CSS } from '../../utils/variables';

export default class CollectionList extends Component {
  render() {
    return (
      <View style={CSS.horizontalPadding15}>
        
      </View>
    );
  }
}
